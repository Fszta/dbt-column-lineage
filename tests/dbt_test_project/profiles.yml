test:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "target/test.db"
      schema: main
      schemas_and_paths:
        main: "target/test.db"
      schema_directory: "target" 