test:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "target/test.db"
      schema: main
      schemas_and_paths:
        main: "{{ env_var('DBT_PROFILES_DIR') }}/test.db"
      schema_directory: "{{ env_var('DBT_PROFILES_DIR') }}"
  target: dev 