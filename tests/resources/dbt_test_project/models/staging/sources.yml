version: 2

sources:
  - name: raw
    schema: main
    tables:
      - name: accounts
        identifier: raw_accounts
      - name: transactions
        identifier: raw_transactions
      - name: countries
        identifier: raw_countries